        VARIABLES   


 char :                     8 bits
 short :                    16 bits
 int :                      32 bits
 float :                    32 bits
 long :                     dépend du hardware et du software
 long long :                64 bits
 double :                   64 bits
 éviter les confusions :    int8_t, uint8_t, int16_t, uint16_t, int32_t, uint32_t, int64_t, uint64_t        (à récupérer dans la bibliothèque stdint.h)







        OPERATEURS
    +                       addition
    -                       soustraction
    *                       multiplication
    /                       division : Attention, ne donne que le quotient pour la division de 2 entiers
    %                       modulo : donne le reste de la division euclidienne, n'a de sens que pour des entiers
    ++                      incrémentation de 1
    --                      décrémentation de 1
    ++a                     incrémentation avant utilisation de a 
    a++                     incrémentation après utilisation de a 
    <,<=,>,>=               comparaison
    ==,!=                   égal et différent
    =                       affectation 
    &&                      ET logique
    ||                      OU logique
    !                       NON logique
    &                       ET bit à bit 
    |                       OU bit à bit 
    ^                       NON OU bit à bit 
    ~                       NON bit à bit
    -                       MOINS 
    a<<X                    décalage des bits vers la gauche de X places
    a>>X                    décalage des bits vers la droite de X places







        OPERATEUR TERNAIRE 

    ?                       if 
    :                       else

    exemple :               a=1 ? b='hello' : b='goodbye'







        CASCADE DE IF ELSE 

switch(expression)
{
    case constante1 : instruction 1 ou bloc 1 d'instructions
                      break;
    case constante2 : instruction 2 ou bloc 2 d'instructions
                      break;
    case constante3 : instruction 3 ou bloc 3 d'instructions
                      break;
    default         : instruction 5 ou bloc 5 d'instructions
                      break;
}

Si la valeur de expression est constante1, l'instruction1 (ou le bloc1) s'exécute puis l'instruction break provque la sortie de la structure switch.
Si aucune constante ne correspond à la valeur de l'expression, c'est l'instruction 5 (ou le bloc 5, derrière default) qui est exécuté. La ligne default n'est pas obligatoire.






        BOUCLES 

    for (int i = 0,i<=N,i++){}          boucle d'incrémentation
    while(...){...}                     boucle while
    do ... while(instruction)           boucle while mais qui fait une première itération avant de vérifier la condition






A METTRE DANS LA FONCTION PRINTF

on met d'abord notre string, et après les %... servent à 

l'élément de texte %c affiche le paramètre (une variable de type entier) correspondant en ascii,
l'élément de texte %d affiche le paramètre (une variable de type entier) correspondant en décimal signé,
l'élément de texte %u affiche le paramètre (une variable de type entier) correspondant en décimal non signé,
l'élément de texte %X affiche le paramètre (une variable de type entier) correspondant en hexadécimal,
l'élément de texte %4.2f affiche le paramètre (une variable de type réel) correspondant en réel aligné sur 4 caractères, avec 2 décimal.





TRANSTYPAGE

Méthode pour réaffecter un type à une variable qui en avait déjà un

int main()
{
    uint8_t a=5,b=22;
    float c;
    c=b/a;
    printf("division sans transtypage %4.2f\n",c);
    c=(float)b/a;
    printf("division sans transtypage %4.2f\n",c);}

le premier print renvoie 4.00 alors que le deuxieme renvoie 4.40. Dans le premier cas on converti en flotant après la division alors que dans le deuxieme cas on converti avant la division






LES POINTEURS 

&variable                               renvoie l'adresse mémoire de variable
setvbuf(stdout,NULL,_IONBF,0);          ligne pour eviter que l'OS ne stocke les caractères dans un buffer avant de les afficher tous ensemble.





LES TABLEAUX 

uint8_t mon_tableau[3]={}                       initialisation d'un tableau avec 3 valeurs nulles
uint8_t mon_tableau[3]={2,4,7}                  initialisation tableau 3 valeurs données
sizeof(mon_tableau)/sizeof(mon_tableau[0])      donne la taille du tableau (marche pour tout type d'objet en fait)
tableau [i]=0 -> if (tableau){print ...}        équivalent à faire des tableau.remove()
int fonction(int tableau[])                     transforme le tableau en un pointeur vers le premier indice de celui-ci

si on appelle un tableau dans une fonction ca devient un pointeur, donc on ne peut pas utiliser la méthode sizeof, c'est que dans la fonction ou la table est déclarée
astuce pour contourner ça : 
    int divisible_par(int k, int liste_entiers[], size_t N){
    for (size_t i = k; i <= N; i++){
        ...}



LES STRUCTURES 

Typedef signifie que l'on crée un nouveau type de variable.

typedef struct {double latitude; double longitude; int16_t altitude;}CoordGPS;
int main()
{
    CoordGPS CoordonneesArcueil={48.8036,2.3317,61};
    printf("Coordonnées d'Arcueil %f %f %d\n",   CoordonneesArcueil.latitude,
                                                 CoordonneesArcueil.longitude,
                                                 CoordonneesArcueil.altitude);

    return 0;
}


Une structure peut contenir des tableaux, des chaînes de caractères et même des structures.

typedef struct {double latitude; double longitude; int16_t altitude;}CoordGPS;
typedef struct {char nom[30];CoordGPS coordonnees; int nb_habitants;}ville;
int main()
{
    ville Arcueil={"Arcueil",{48.8036,2.3317,61},20120};
    printf("Coordonnées de %s %f %f %d\n",  Arcueil.nom,
                                            Arcueil.coordonnees.latitude,
                                            Arcueil.coordonnees.longitude,
                                            Arcueil.coordonnees.altitude);
    return 0;
}

